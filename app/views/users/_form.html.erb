<%= form_for(user) do |f| %>
<fieldset>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.label :username %>
  <%= f.text_field :username, size: "100x6" %>
  <br>
  <%= f.label :password %>
  <%= f.text_field :password, size: "100x6" %>

  <div class="actions">
    <%= f.submit %>
  </div>
  </fieldset>
<% end %>

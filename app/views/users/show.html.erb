<p id="notice"><%= notice %></p>

<h2> <%= @user.username %> </h2>

<h2><%= @user.username %>'s Boats </h2>

<% @user.boats.each do |boat| %>
	<p><%= link_to Boat.boat.id, boat_path(boat) %></p>
<% end %>


<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
